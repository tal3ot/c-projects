#include <stdio.h>
#include <stdlib.h>
#include <assert.h>


int main (){
    int arry [] = {1, 2, 3, 4, 5, 6};
    int c;
    for (c = 5; c >= 0; c--){ // c is the num of the term in the arrey
       printf(" %d ", arry[c]);
    }
}

